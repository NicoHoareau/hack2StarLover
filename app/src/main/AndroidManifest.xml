<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.nicolashoareau_toulousewcs.hack2starlover">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity" />

        <activity android:name=".LoginActivity">

        </activity>

        <activity android:name=".ChooseSideActivity" />

        <activity android:name=".MatchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChoosePlanetActivity"></activity>

        <activity android:name=".ProfileActivity" />

        <activity android:name=".SeeLoversActivity" />

        <activity android:name=".ChooseLoverActivity">
        </activity>


        <activity android:name=".NewProfilActivity" />

        <activity android:name=".TchatActivity">

        </activity>

        <activity android:name=".TchatRoomActivity"/>

    </application>

</manifest>